#!/bin/bash

NOW=${NOTICE_DATE-`date`}
DELIMITER=${NOTICE_SEP-' -- '}
LOG=${NOTICE_LOG-"$HOME/.notice"}

echo -n "${NOW}${DELIMITER}" >> $LOG

if [ $# -gt 0 ]; then
    echo "${@}" >> $LOG
else
    cat - >> $LOG
    echo "$DELIMITER" >> $LOG
fi
